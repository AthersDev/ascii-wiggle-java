# ascii-wiggle-java
This is a Java program which animates wiggle effect in your terminal.

## Usage
 1. Compile it by running `javac path/Wiggle.java`.
 2. Then execute it: `java Wiggle`.
 3. You may customise it by giving arguments: `java Wiggle [text] [length] [maxIndent] [rows]`
